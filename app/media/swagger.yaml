---
swagger: '2.0'
info:
  description: |
    ## Welcome
    Welcome to my Curriculum Vitae API Reference documentation !
    You can use the API to integrate with 3rd-party systems and custom applications.
    This document provides an overview of how to use the API.
    ## A Quick Note
    This API is intended for software developers, and those are who are comfortable working with RESTful APIs, and writing some code.
    ## Authentication
    No Authentication is required to access this API

  version: 1.0.0
  title: Curriculum Vitae REST API
host: ''
basePath: "sogloarcadius.fr/portfolio/api"
schemes:
- http
- https
paths:
  "/resume":
    get:
      tags:
      - resume
      summary: Download my resume in JSON Format
      produces:
      - application/json
      responses:
        '200':
          description: resume in json format

  "/resume/biography":
    get:
      tags:
      - resume
      summary: Download resume biography in JSON Format
      produces:
      - application/json
      responses:
        '200':
          description: resume biography in json format
  "/resume/languages":
    get:
      tags:
      - resume
      summary: Download resume languages in JSON Format
      produces:
      - application/json
      responses:
        '200':
          description: resume languages in json format
  "/resume/educations":
    get:
      tags:
      - resume
      summary: Download resume educations in JSON Format
      produces:
      - application/json
      responses:
        '200':
          description: resume educations in json format          
  "/resume/experiences":
    get:
      tags:
      - resume
      summary: Download resume experiences in JSON Format
      produces:
      - application/json
      responses:
        '200':
          description: resume experiences in json format 
  "/resume/skills":
    get:
      tags:
      - resume
      summary: Download resume skills in JSON Format
      produces:
      - application/json
      responses:
        '200':
          description: resume skills in json format   
  "/resume/interests":
    get:
      tags:
      - resume
      summary: Download resume interests in JSON Format
      produces:
      - application/json
      responses:
        '200':
          description: resume interests in json format      
  "/projects":
    get:
      tags:
      - project
      summary: Get all my projects information in JSON Format
      description: ''
      produces:
      - application/json
      responses:
        '200':
          description: projects in json format
